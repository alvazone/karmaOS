services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - "3001:3001"

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"

  ai:
    build:
      context: apps/ai
      dockerfile: Dockerfile
    ports:
      - "8000:8000"